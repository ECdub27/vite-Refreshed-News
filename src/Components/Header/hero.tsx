
import BasicTimeline from './timeline';
import Box from '@mui/material/Box';




function HeroSlide() {
 

  return (
    <div>
    <Box>
    <BasicTimeline key={''} message={''} count={0} disabled={false} names={[]} status={'waiting'} description={''} title={''} name={''} style={undefined} position={''} urlToImage={''} rowHeight={0} flexItems={0} href={''} onClick={function (): void {
          throw new Error('Function not implemented.');
        } } setState={function (): void {
          throw new Error('Function not implemented.');
        } } />
    </Box>
    </div>
  );
}

export default HeroSlide;